node_modules/
.registry/

# Project-local mod runtime state (generated by the mods, not source)
.agents/
.commandcode/

# Personal runtime data — never relevant to the repo
# (cache-tracker hit-rate data, learning stores, receipts)
cache-tracker.jsonl
*.jsonl

# Separately-versioned kit — lives on disk, never published here
_templates/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
